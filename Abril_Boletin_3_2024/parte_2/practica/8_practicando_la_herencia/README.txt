RTA:

1. El color del parrafo siempre sera el ultimo valor que tenga asignado,
en este caso en el archivo main.css en la linea 11 se asgina el color verde.

2. Porque en el CSS se lee de arriba hacia abajo y de izquierda a derecha.

3. En ese caso el color del parrafo sera azul, ya que lo hereda de su contenedor padre la etiqueta <div>.